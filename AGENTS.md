# Fourseasquant 智能体说明

## 开始工作前

1. 完整阅读仓库根目录的 `Mac开发环境_AGENT交接.md`，了解开发环境并充分利用 Apple M5 芯片资源。
2. 交接文档中的版本、登录状态和后台服务状态可能变化，执行关键任务前必须重新核验。
3. 只使用简体中文沟通；不可避免的其他语言术语必须附简体中文解释。
4. 不得读取、输出、保存或提交密码、API 密钥、访问令牌及其他秘密。

## Agent skills

### Issue tracker

问题和产品需求记录在 GitHub 私有仓库 `zongeli65-lang/Fourseasquant` 的 Issues 中。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用五个默认问题分流标签。详见 `docs/agents/triage-labels.md`。

### Domain docs

采用单上下文领域文档结构。详见 `docs/agents/domain.md`。
