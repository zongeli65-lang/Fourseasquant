{
  "name": "fourseasquant",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "uv run python scripts/dev.py",
    "data:history-year": "uv run python scripts/import_one_year_history.py",
    "start": "npm run build --workspace frontend && uv run python scripts/start.py",
    "build": "npm run build --workspace frontend",
    "typecheck": "npm run typecheck --workspace frontend && tsc --project tsconfig.tests.json --noEmit --pretty false && uv run mypy",
    "test:browser": "playwright test",
    "test": "npm run build --workspace frontend && uv run pytest && npm run test:browser"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
